{
  "name": "asteroids-canvas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=/asteroids-canvas-game-typescript/",
    "preview": "vite preview --base /asteroids-canvas-game-typescript/"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
